services:
  status:
    image: twinproduction/gatus:latest
    restart: unless-stopped
    networks:
      - proxy
    volumes:
      - ./config:/config

networks:
  proxy:
    name: cicd_proxy
    external: true